INICIO
    int a;
    int b;
    int resultado;

    receba a;
    receba b;

    // Verifica se são iguais
    resultado = a == b;
    devolva "a == b: ", resultado;

    // Verifica se são diferentes
    resultado = a != b;
    devolva "a != b: ", resultado;

    // Verifica se a > b
    resultado = a > b;
    devolva "a > b: ", resultado;

    // Verifica se a < b
    resultado = a < b;
    devolva "a < b: ", resultado;

    // Verifica se a >= b
    resultado = a >= b;
    devolva "a >= b: ", resultado;

    // Verifica se a <= b
    resultado = a <= b;
    devolva "a <= b: ", resultado;

FIM
